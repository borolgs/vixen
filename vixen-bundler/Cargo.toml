[package]
name = "axum-vixen-bundler"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Build-script half of vixen: drives bun over the page entrypoints and hands rustc the manifest."
keywords = ["axum", "bun", "bundler", "assets", "build-script"]
categories = ["web-programming", "development-tools::build-utils"]

[lib]
name = "vixen_bundler"

[lints]
workspace = true

[dependencies]
